{
  "_comment": "Build settings only — NO SECRETS IN THIS FILE. Signing credentials live in voltbuilder.local.json (git-ignored); scripts/package-voltbuilder.sh merges them into the uploaded zip. See README → Building for a device.",
  "build": "release",
  "platforms": [
    "android"
  ]
}
